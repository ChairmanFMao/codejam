#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define sz(c) (int)c.size()
#define all(c) c.begin(),c.end()

void solve() {
	int n, p;
	cin >> n >> p;
	vector<vector<int> > x(n, vector<int>(p));
	for (int i = 0; i < n; i++)
		for (int j = 0; j < p; j++)
			cin >> x[i][j];

	ll out = 0, current = 0;
	for (int i = 0; i < n; i++) {
		if (i & 1) {

		} else {
			out += *max_element(all(x[i]))
			current += *max_element(all(x[i]))
		}
	}
}

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int t;
	cin >> t;
	for (int i = 1; i <= t; ++i) {
		cout << "Case #" << i << ":\n";
		solve();
	}
}

